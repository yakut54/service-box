<?php

/**
 * Комиссия платформы — переход с тарифов на плоскую ставку (см. PLAN.md).
 * Одна ставка на всех шоперов, без исключений и настроек по магазину.
 */
return [
    'commission_percent' => 20,

    // Минимальная сумма заказа — вместо минимальной комиссии в рублях
    // (при 20% с 1000₽ комиссия уже сама по себе разумная, ~200₽).
    'min_order_amount_kopecks' => 100000,

    // Потолок точек на одного владельца сети — каждая точка необратимо
    // занимает отдельную Postgres-схему (~30 таблиц), лимит защищает от
    // угнанного аккаунта, заваливающего базу созданием схем.
    'max_shops_per_chain_owner' => 20,
];
