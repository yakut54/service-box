/// Настройки приложения, которые могут понадобиться в одном месте.
class AppConfig {
  const AppConfig._();

  /// true — работаем на моковых данных (см. MockShopRepository).
  /// Бэкенд сейчас не поднят, поэтому по умолчанию всегда true.
  /// Когда сервер появится — переключим на false прямо здесь.
  static const bool useMocks = true;

  /// Цвет приложения, пока ни один магазин ещё не выбран
  /// (экран онбординга — там ещё нет "своего" цвета магазина).
  static const int defaultColor = 0xFF2563EB; // синий
}
