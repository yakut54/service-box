# ServiceBox — Plan

## Стек

- **Backend:** Laravel 11, PostgreSQL 16 (multi-tenant schemas), Redis, Sanctum
- **Frontend:** Vue 3 + TypeScript, Tailwind CSS (admin), Pinia, Vite
- **Infrastructure:** Docker, Nginx, GitHub Actions (deploy on push to `new-branch`)
- **Server:** root@31.128.39.216, app `/var/www/servicebox/`, домен `https://yakut54.ru`
- **Queue driver:** `sync` (нет async-очередей — все операции синхронные)

---

## Архитектура

```
servicebox/
├── app/              — Laravel API
├── admin/            — SPA для владельца магазина (Vue 3)
├── widget/           — Встраиваемый виджет для сайта клиента (Vue 3, Shadow DOM)
└── database/         — Миграции (глобальные + функция создания tenant-схемы)
```

Каждый магазин — отдельная PostgreSQL-схема `shop_{id}`. Таблицы `customers`, `products`, `orders`, `bookings`, `masters` и др. изолированы per-tenant.

---

## Статус по модулям

### ✅ Готово

| Модуль | Что есть |
|---|---|
| Auth | Регистрация, логин, смена пароля, reset |
| Shop | Настройки магазина, часы работы, timezone (38 зон) |
| Categories | CRUD, drag-n-drop сортировка |
| Products | Физические / цифровые / услуги, фото, цена, акция |
| Orders | Создание, статусы, оплата YooKassa, webhook |
| Customers | Список, детальная карточка, удаление |
| Bookings | Слоты, бронирование через виджет, статусы |
| Masters | CRUD, привязка услуг, расписание |
| Discounts | Промокоды, автоприменение, история использования |
| Reviews | Отзывы с рейтингом через виджет |
| Payments | YooKassa, подписки, история платежей |
| Legal | Документы (оферта, конфиденциальность) на домене магазина |
| Superadmin | Список магазинов, тарифы, выручка платформы |
| Widget | Каталог, корзина, checkout, мои заказы, мои записи |

---

## Telegram-бот (@sb_widget_bot)

**Токен:** `8527648051:AAGPvspeys1_ssPc_5pZAoZ-Mcuwv9l0ioY`
**Webhook:** `https://yakut54.ru/api/webhook/telegram`
**allowed_updates:** `["message", "callback_query"]`

### Этапы

| # | Что | Статус |
|---|---|---|
| 1 | Подключение бота к магазину (код из настроек → /start КОД) | ✅ Готово |
| 1 | Отключение бота с confirm-диалогом | ✅ Готово |
| 2 | Уведомление владельцу при новой записи (кнопки Подтвердить/Отменить) | ✅ Готово |
| 2 | Уведомление владельцу при новом заказе (кнопки Подтвердить/Отменить) | ✅ Готово |
| 3 | Клиент привязывает Telegram после бронирования (deep-link кнопка в виджете) | ✅ Готово |
| 4a | Клиент получает уведомление когда владелец подтверждает/отменяет запись | ✅ Готово |
| 4b | Напоминание клиенту за 24ч и 2ч до записи | ⏳ Не сделано |
| 5 | Запрос оценки клиенту через 2ч после окончания записи | ⏳ Не сделано |
| 6 | Plan gate — Telegram только на платном тарифе | ⏳ Не сделано |

### Этап 4b — Напоминания (детали)

Так как queue driver = `sync`, нужен:
1. Artisan-команда `php artisan bookings:send-reminders`
2. Laravel Scheduler (`app/Console/Kernel.php`) — запуск каждые 5 минут
3. Cron в Docker: `* * * * * php artisan schedule:run`
4. Команда проходит по всем shop_* схемам, находит записи через 24ч ± 15мин и 2ч ± 15мин, отправляет сообщение в Telegram клиента (если привязан)

### Этап 5 — Оценка после визита (детали)

1. Та же команда-шедулер: ищет записи с `status=completed` и `end_time` было 2ч назад
2. Отправляет клиенту inline-клавиатуру с оценкой 1–5 ⭐
3. Webhook обрабатывает `callback_query` вида `rate:{bookingId}:{score}`
4. Сохраняет review в `shop_{id}.reviews`

---

## UX-улучшения (не сделаны)

| Что | Сложность | Приоритет |
|---|---|---|
| Автополлинг в админке (обновление без перезагрузки страницы) | Низкая (30 мин) | Средний |
| CSV-экспорт заказов и клиентов | Средняя | Низкий |

---

## Бизнес-логика (не сделано)

| Что | Зависит от |
|---|---|
| Ограничения по тарифу (plan gates) для Telegram, CSV, виджета | Тарифная сетка |
| Кастомный домен для виджета (Business+) | Инфраструктура |

---

## Деплой

1. Пуш в ветку `new-branch`
2. GitHub Actions → SSH на сервер → `git pull` + `composer install` + `php artisan migrate` + перезапуск контейнера
3. Время деплоя: ~1 минута
4. Фронт (admin/widget) деплоится отдельно при изменениях в `admin/` или `widget/`

---

## Что сейчас (следующий шаг)

**Этап 4b — Напоминания клиентам** или **Автополлинг в админке** — на выбор.
